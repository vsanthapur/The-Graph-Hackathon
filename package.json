{
  "name": "eth-subgragh-hackathon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ eth-subgragh-hackathon",
    "create-local": "graph create --node http://localhost:8020/ eth-subgragh-hackathon",
    "remove-local": "graph remove --node http://localhost:8020/ eth-subgragh-hackathon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 eth-subgragh-hackathon",
    "test": "graph test"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
